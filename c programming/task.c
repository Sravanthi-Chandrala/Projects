//Finding the area of a square by using the side of square
# include <stdio.h>
int main(){
    int side;
    printf("enter side:");
    scanf("%d",&side);
    int area=side*side;
    printf("area is: %d",area);
    return 0;
}


# include <stdio.h>
int main(){
    float side;
    printf("enter side:");
    scanf("%f",&side);
    float area=side*side;
    printf("area is: %f",area);
    return 0;
}


//Finding the area of a cicle by using radius
# include <stdio.h>
int main(){
    int radius;
    printf("enter radius:");
    scanf("%d",&radius);
    int pi=3.14;
    printf("area is: %d",pi*radius*radius);
    return 0;
}



# include <stdio.h>
int main(){
    float radius;
    printf("enter radius:");
    scanf("%f",&radius);
    float pi=3.14;
    printf("area is: %f",pi*radius*radius);
    return 0;
}